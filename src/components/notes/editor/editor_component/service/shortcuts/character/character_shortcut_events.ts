import type { CharacterShortcutEvent } from './character_shortcut_event';

// Standard character shortcut events (matching Flutter standardCharacterShortcutEvents)
export const standardCharacterShortcutEvents: CharacterShortcutEvent[] = [
  // Add standard character shortcuts here
  // This would include things like markdown shortcuts, auto-formatting, etc.
];